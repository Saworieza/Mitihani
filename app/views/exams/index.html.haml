%h1 Showing exams

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Term
      %th
      %th
      %th

  %tbody
    - @exams.each do |exam|
      %tr
        %td= link_to exam.name, exam
        %td= exam.term.term_name
        
%br

= link_to 'New Exam', new_exam_path
