.nested_fields 
    %tr
        %td= mark.collection_select :student_id, Student.all, :id, :stud_name, include_blank: true
        -# %td= mark.grouped_collection_select :student_id, Stream.order(:name), :students, :full_name, :id, :stud_name, include_blank: true
        %td= mark.text_field :mark
        %td= link_to_remove_association "remove mark", f