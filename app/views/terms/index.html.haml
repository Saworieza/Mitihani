%h1 Listing terms
= link_to 'New Term', new_term_path

%table.table.table-striped
  %thead
  %tbody
    -# - @terms.each do |term|
    - @terms.group_by(&:year).each do |year, terms|
      %tr
        %th.topline #{link_to year.year, year} Academic Terms
      %tr
      -terms.each do |term|
        %td= link_to term.name, term

%br

